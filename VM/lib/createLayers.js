export function createLayers(type, nbLayers, comp) {
    
    var layers = [];
    
    for (var i = 0; i <= nbLayers; i++){
        layers.push(comp.layers.addText("dlfgjsdk"))
    }
    return layers;
}
